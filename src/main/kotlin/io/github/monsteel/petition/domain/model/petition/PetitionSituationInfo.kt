package io.github.monsteel.petition.domain.model.petition

data class PetitionSituationInfo(val agreeCount: Int,
                                 val answerCount: Int,
                                 val awaitingPetitionCount: Int)